import yarel_lang_recursion_sp0.*
/* Just to copycat the structure of isLessThanOne.
 * In fact for the dicotomic recursion we must not
 * stop dividing by 2 if the dividend is odd.     */
module yarel_lang_recursion_sp1 {
	dcl sp1 : 6 int
	def sp1 := 
	 /*n e o nhalf s.t. n=2*nhalf+o execHalf nexecHalf*/
	 id|id|id|id|id|id
	 /*n e o nhalf s.t. n=2*nhalf+o execHalf nexecHalf*/
}